# DAILY REPORT - 03/04/2026

**Dự án:** Xây dựng Website Bán Trang Sức 
**Nhóm:** Lê Minh Quân (Nhóm trưởng) • Trần Quỳnh Anh • Trần Minh Nguyệt

---

## 1. TỔNG HỢP TIẾN ĐỘ HÔM NAY 03/04/2026

| Thành viên | Chức năng phụ trách | Trạng thái | Ghi chú |
| :--- | :--- | :---: | :--- |
| Lê Minh Quân (Nhóm trưởng) | Backend: Khởi tạo Laravel, thiết kế ERD & Migration (5 bảng), xây dựng Routing, xây dựng Model & Eloquent ORM | 3 ✅ / 1 🔄 / 0 ⏳ → **75%** | Đang xử lý N+1 query problem trong Model Eloquent khi load danh sách sản phẩm kèm danh mục. |
| Trần Quỳnh Anh | Frontend Blade: Layout chính, trang Danh sách sản phẩm, trang Chi tiết sản phẩm, chức năng Tìm kiếm & Lọc | 2 ✅ / 1 🔄 / 1 ⏳ → **50%** | Đang kết nối nút 'Thêm vào giỏ hàng' với CartController, xử lý luồng Session. Chức năng lọc chưa bắt đầu. |
| Trần Minh Nguyệt | Admin Panel: Layout sidebar, CRUD Sản phẩm trang sức, Upload ảnh, CRUD Danh mục sản phẩm | 1 ✅ / 1 🔄 / 2 ⏳ → **25%** | Form Update sản phẩm đang lỗi 422 do validation rule `'image' => 'required'` bị fail khi không đổi ảnh. |
| **TỔNG NHÓM** | Xây dựng song song: Backend MVC (Quân) + Blade Views (Quỳnh Anh) + Admin Panel (Nguyệt) | 6 ✅ / 3 🔄 / 3 ⏳ → **50%** | Tiến độ đúng kế hoạch. Cần phối hợp kết nối CartController (Quân + Quỳnh Anh) trong tuần tới. |

---

## 2. CHI TIẾT NHIỆM VỤ TỪNG THÀNH VIÊN

| Thành viên | Chức năng phụ trách | Trạng thái | Ghi chú |
| :--- | :--- | :---: | :--- |
| **Lê Minh Quân** `BE-01` | Khởi tạo dự án Laravel 10, cấu hình `.env` (DB_HOST, DB_NAME, APP_KEY, APP_URL), thiết lập cấu trúc thư mục MVC, tạo symlink storage | ✅ Hoàn thành | `php artisan key:generate` và `storage:link` chạy thành công. Kết nối MySQL ổn định. Cấu trúc: `app/Models`, `app/Http/Controllers`, `app/Http/Middleware`, `resources/views`. |
| **Lê Minh Quân** `BE-02` | Thiết kế ERD và viết Migration cho 5 bảng: `users`, `categories`, `products`, `orders`, `order_items` – định nghĩa kiểu dữ liệu, khóa ngoại, index | ✅ Hoàn thành | `products`: name, slug (unique), price (decimal 12,2), material (enum: vàng/bạc/đá quý/bạch kim), stock, image, category_id (FK). `orders`: user_id (FK), total_price, status (enum: pending/confirmed/shipping/delivered/cancelled), shipping_address. `php artisan migrate` không lỗi. |
| **Lê Minh Quân** `BE-03` | Xây dựng hệ thống Routing (`web.php`): phân luồng `Route::prefix('admin')` với middleware `CheckAdmin`, Route nhóm người dùng gồm `/`, `/products`, `/products/{slug}`, `/cart`, `/orders` | ✅ Hoàn thành | Admin routes bảo vệ bởi middleware `CheckAdmin` (kiểm tra `role = 'admin'`). User routes public, trừ `/cart` và `/orders` yêu cầu auth. Named routes đặt đầy đủ để dùng `route()` helper trong Blade. |
| **Lê Minh Quân** `BE-04` | Xây dựng Model Eloquent: `Product`, `Category`, `Order`, `OrderItem`, `User` – định nghĩa relationships (`hasMany`, `belongsTo`) và local scope lọc theo chất liệu, khoảng giá | 🔄 Đang xử lý | Đã xong: `Product::belongsTo(Category)`, `Category::hasMany(Product)`, `Order::hasMany(OrderItem)`. Đang xử lý N+1 query – nghiên cứu `Product::with('category')->paginate(12)` để tối ưu. |
| **Trần Quỳnh Anh** `FE-01` | Xây dựng Blade layout chính (`app.blade.php`): `@yield('content')`, `@stack('scripts')`, chia sẻ biến `$categories` và `$cartCount` toàn cục qua `ViewServiceProvider` | ✅ Hoàn thành | `ViewServiceProvider::boot()` dùng `View::share()` để inject `$categories` và `$cartCount` vào tất cả View. Header hiển thị động số lượng sản phẩm trong giỏ hàng lấy từ `Session::get('cart')`. |
| **Trần Quỳnh Anh** `FE-02` | Xây dựng trang Danh sách sản phẩm trang sức: kết nối `ProductController@index`, hiển thị grid card (ảnh, tên, chất liệu, giá), phân trang với `paginate(12)` | ✅ Hoàn thành | Blade dùng `@forelse` để render card sản phẩm, `@empty` hiển thị thông báo 'Không có sản phẩm'. `$products->links()` render phân trang tự động. Hiển thị đúng tên danh mục (nhẫn, vòng, dây chuyền) và chất liệu từ DB. |
| **Trần Quỳnh Anh** `FE-03` | Xây dựng trang Chi tiết sản phẩm: hiển thị đầy đủ thông tin (tên, giá, chất liệu, mô tả, tồn kho), nút 'Thêm vào giỏ hàng', block sản phẩm liên quan cùng danh mục | 🔄 Đang xử lý | Đang xử lý phần sản phẩm liên quan: lấy 4 sản phẩm cùng `category_id`, loại trừ sản phẩm hiện tại bằng `where('id', '!=', $product->id)`. Nút 'Thêm vào giỏ hàng' chưa kết nối được với `CartController`. |
| **Trần Quỳnh Anh** `FE-04` | Xây dựng chức năng Tìm kiếm & Lọc sản phẩm: lọc theo danh mục (nhẫn/vòng/dây chuyền/bông tai), lọc theo chất liệu (vàng/bạc/đá quý), lọc theo khoảng giá | ⏳ Chưa bắt đầu | Dự kiến dùng Eloquent query scope kết hợp `request()->query()`. URL: `/products?category=nhan&material=vang&min_price=500000&max_price=5000000`. Cần phối hợp với Quân để `ProductController@index` xử lý các điều kiện `where` linh hoạt. |
| **Trần Minh Nguyệt** `AD-01` | Xây dựng Admin Panel layout (`admin.blade.php`): sidebar điều hướng (Sản phẩm, Danh mục, Đơn hàng), header hiển thị tên admin, active state theo route hiện tại | ✅ Hoàn thành | Layout admin hoàn toàn tách biệt với layout người dùng. Sidebar dùng `request()->routeIs('admin.products.*')` để highlight menu đang active. Toàn bộ trang quản trị dùng `@extends('layouts.admin')`. |
| **Trần Minh Nguyệt** `AD-02` | Xây dựng CRUD Sản phẩm trang sức (Admin): danh sách có phân trang, form thêm/sửa gồm các trường tên, giá, chất liệu, danh mục, mô tả, tồn kho, ảnh – xóa có hộp thoại xác nhận | 🔄 Đang xử lý | Create và Read đã hoạt động. Update đang lỗi 422: validation rule `'image' => 'required\|image'` bị fail khi admin sửa thông tin mà không đổi ảnh. Cần đổi thành `'nullable\|image\|mimes:jpg,jpeg,png\|max:2048'` cho `UpdateProductRequest`. Delete chưa thực hiện. |
| **Trần Minh Nguyệt** `AD-03` | Tích hợp chức năng Upload ảnh sản phẩm trang sức: lưu file vào Storage Laravel (`storage/app/public/products/`), hiển thị preview ảnh trong form, cập nhật đường dẫn ảnh vào bảng `products` | ⏳ Chưa bắt đầu | Dự kiến: `$request->file('image')->store('products', 'public')` → lưu đường dẫn vào cột image. Hiển thị bằng `asset(Storage::url($product->image))`. Cần xử lý xóa ảnh cũ khi cập nhật: `Storage::disk('public')->delete($oldPath)`. |
| **Trần Minh Nguyệt** `AD-04` | Xây dựng CRUD Danh mục sản phẩm trang sức (Admin): thêm/sửa/xóa danh mục (nhẫn, vòng tay, dây chuyền, bông tai...), kiểm tra ràng buộc khóa ngoại trước khi xóa | ⏳ Chưa bắt đầu | Khi xóa danh mục cần kiểm tra: `if ($category->products()->count() > 0)` → không cho xóa, trả về thông báo lỗi 'Danh mục đang có sản phẩm, không thể xóa'. Validation tên danh mục: unique trong bảng `categories`. |


---

*📌 Báo cáo ngày 03/04/2026 | Dự án: Website Bán Trang Sức Nhóm: Lê Minh Quân – Trần Quỳnh Anh – Trần Minh Nguyệt*
